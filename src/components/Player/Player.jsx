import React from 'react';
// import PropTypes from 'prop-types';

import './style.css';

const Player = () => <div className="container" />;

// Player.propTypes = {
//   increaseLevel: PropTypes.func.isRequired,
// };

export default Player;
